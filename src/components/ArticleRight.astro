---
    interface Props{
        id: string;
        title: string;
        description: string;
        image: string;
    }

    const {id, title, description, image} = Astro.props;
---

<article id={id} class="w-full bg-slate-800 p-8 flex flex-row-reverse justify-between items-center gap-x-5 max-md:flex-col max-md:gap-y-5 mt-20">
    <div class="max-w-[37rem] max-lg:max-w-[25rem] max-md:max-w-full flex flex-col gap-y-5 justify-center items-start">
        <h1 class="bg-gradient-to-br from-yellow-300 to-yellow-600 bg-clip-text text-transparent text-4xl font-bold mb-4">
            {title}
        </h1>

        <p class="text-gray-100 text-lg">
            {description}
        </p>
    </div>

    <div class="w-fit h-fit">
        <img src={image} alt="Proceso en línea" class="w-[25em] max-lg:w-[20rem] h-auto drop-shadow-lg" />
    </div>
</article>
